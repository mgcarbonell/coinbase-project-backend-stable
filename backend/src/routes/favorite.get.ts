import express from "express"

const router = express.Router()
// all currencies
// .find retrieves an array

// individual currency
// findOne might be by ID, wonder if I can req.params
/*
if (!favorite) {
  return res.json ({
  message: "no favorites found"
  })
}
*/
